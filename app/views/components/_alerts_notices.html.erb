<% if notice.present? %>
  <div class="notice">
    <p><%= notice %></p>
  </div>
<% end %>
<% if alert.present? %>
  <div class="alert">
    <p><%= alert %></p>
  </div>
<% end %>
<% if notice.present? || alert.present? %>
  <%= javascript_pack_tag "alerts_notices" %>
<% end %>